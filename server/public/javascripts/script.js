var urlServices = 'services.php';// void service(string service, {} opts, function callback)// Call a server service// 	- string service : name of the service//	- {} opts : the parameters//	- function callback : the function to call when it's donefunction service(service, opts, callback) {	opts.s = service;	var url = urlServices + '?' + $.param(opts);	$.get(url, callback);}function addEmail(_email) {	var opts = {		email : _email	};		service('add_email', opts, function(res) {		if(res == 'OK')			alert('Merci, votre adresse a bien été enregistrée ! A bientôt !');	});}